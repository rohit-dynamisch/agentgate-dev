# AgentGate — AI Development Model

**This content moved to `/WORKFLOW.md` at the repository root (2026-09-13)** — that's now the
single canonical description of roles, the checkpoint loop, documentation conventions, and the
per-checkpoint codebase-documentation requirement, written to be self-contained enough to share
directly with the Lead Architect (ChatGPT).

This file is kept as a redirect stub, not deleted, so existing references to this path
(`docs/DEVELOPMENT/AI_DEVELOPMENT_MODEL.md`) keep working without needing to be rewritten.

**Read `/WORKFLOW.md` instead of this file.**
